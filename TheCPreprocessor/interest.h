

#ifndef INTEREST_H
#define INTEREST_H

// Macro to calculate Simple Interest
#define SIMPLE_INTEREST(principal, rate, time) ((principal) * (rate) * (time) / 100.0)

// Macro to calculate Amount
#define AMOUNT(principal, interest) ((principal) + (interest))

#endif
